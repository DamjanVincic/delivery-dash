services:
  db:
    image: postgres:13
    container_name: db-deliverydash
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
